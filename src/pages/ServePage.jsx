import PageShell from '../components/PageShell'

export default function ServePage() {
  return (
    <PageShell title="Volunteer interest form">
      <p className="placeholder">
        Public volunteer form (Phase 6). Serving areas and submission will connect to
        the API here.
      </p>
    </PageShell>
  )
}
